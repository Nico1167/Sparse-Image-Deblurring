# Sparse-Image-Deblurring
Image deblurring and deconvolution framework using sparsity-promoting optimization (L1 regularization) in the wavelet domain. Features custom implementations of proximal gradient methods (ISTA and FISTA) compared against PyLops benchmarks on real image datasets.
